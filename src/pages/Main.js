import React from "react";
import {Grid, Text, Button} from "../elements/index";

import Card from "../components/Card";

const Main = (props) => {
    return(
        <React.Fragment>
            <Card/>
        </React.Fragment>
    );
}

export default Main;